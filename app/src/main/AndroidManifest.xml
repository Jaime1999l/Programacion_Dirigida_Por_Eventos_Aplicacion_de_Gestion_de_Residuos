<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.DarkActionBar">

        <!-- Declarar la actividad del mapa -->
        <activity
            android:name="com.example.programacion_dirigida_por_eventos_aplicacion_de_gestion_de_residuos.activity.MapActivity"
            android:exported="false" />

        <!-- Declarar la actividad principal -->
        <activity
            android:name="com.example.programacion_dirigida_por_eventos_aplicacion_de_gestion_de_residuos.GestorResiduosMain"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>


